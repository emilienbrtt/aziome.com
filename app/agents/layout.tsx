export const metadata = { title: 'Agents IA — Aziome' };

export default function AgentsLayout({ children }: { children: React.ReactNode }) {
  return <main className="min-h-screen">{children}</main>;
}
